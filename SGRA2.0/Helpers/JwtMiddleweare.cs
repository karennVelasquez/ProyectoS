using SGRA2._0.Model;
using SGRA2._0.Service;
using Microsoft.Extensions.Options;
using Microsoft.IdentityModel.Tokens;
using System.IdentityModel.Tokens.Jwt;
using System.Text;

namespace SGRA2._0.Helpers
{
    public class JwtMiddleweare
    {
       /* private readonly RequestDelegate _next;
        private readonly AppSettings _appSettings;

        public JwtMiddleweare(RequestDelegate next, IOptions<AppSettings> )
        {
            _next = next;
            _appSettings = appSettings.Value;
        }

        public async Task Invoke(HttpContent context, IUserService userService)
        {
            
        }*/
    }
}
